import { EmptyState } from "@/components/shared/empty-state";
export default function Forbidden() { return <EmptyState title="This area needs a different role" description="Your account does not have access. Contact an administrator if you need researcher or admin access." />; }
